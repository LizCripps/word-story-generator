{
  "name": "word-story-generator",
  "version": "1.0.0",
  "description": "AI-powered Word Story Generator — Vercel backend + SiteGround frontend",
  "type": "module",
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
